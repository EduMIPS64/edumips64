# Duck Orchestration Log — 2026-06-08

**Agent:** Duck (rubber-duck)  
**Batch:** Unify build+promote, first production promotion  
**Assignment:** Review workflow design pre-implementation

## Outcome

**Status:** APPROVED (with 3 refinements adopted)

## Summary

Duck (rubber-duck review process) examined the proposed unify build+promote design and provided critical feedback:

### Refinements Proposed and Adopted

1. **Concurrency to promote job**: Moved `web-pages-deploy` concurrency group from global context to promote job specifically. This allows builds to run in parallel when validating external ci.yml runs (run_id mode), avoiding unnecessary serialization.
   - **Tank adoption:** ✓ Implemented in promote-web.yml

2. **Mode-specific promote conditional**: Added explicit `if: ...` conditional logic for mode-specific behavior (build-if-empty vs promote-only-if-set), making the job's execution path immediately clear to future readers.
   - **Tank adoption:** ✓ Implemented with clear SOURCE_RUN_ID logic

3. **Master-only gate on both jobs**: Insisted that both `build` and `promote` jobs enforce `ref_name == 'master'` gate (not just promote), preventing accidental feature-branch workflow execution.
   - **Tank adoption:** ✓ Implemented on both jobs with actor==lupino3 AND ref_name==master

## Validation

All refinements were analyzed for:
- Security impact (gates tightened, not relaxed)
- Operational correctness (concurrency behavior improved)
- Code clarity (explicit conditionals prevent confusion)

## Notes

Rubber-duck review proved valuable for catching subtle workflow logic issues before implementation. All recommendations were technically sound and implemented as proposed.
