# Tank Orchestration Log — 2026-06-08

**Agent:** Tank (general-purpose, background, model sonnet)  
**Batch:** Unify build+promote, first production promotion  
**Assignment:** Implement optional run_id on `.github/workflows/promote-web.yml`

## Outcome

**Status:** SUCCESS  
**Actionlint:** clean (no violations)  
**PR:** #1830 (squash-merged as commit 1d037b23)

## Summary

Tank implemented the core workflow logic for unify build+promote:

1. **Optional `run_id` parameter**: When empty, a `build` job (reusable `build-web.yml`, no secrets) builds current master; when set, validates and promotes an external ci.yml/master/success run.

2. **Security gates**: Both `build` and `promote` jobs gated on actor==lupino3 AND ref_name==master. Build job has `contents: read` only (never sees PAT_WEBUI).

3. **Unified retry/fail logic**: Single `SOURCE_RUN_ID` variable handles both current and external artifact downloads, with shared retry + fail-closed semantics.

4. **Concurrency optimization**: Moved `web-pages-deploy` concurrency group to promote job so builds aren't serialized when run_id is set.

5. **Rollback preserved**: run_id mode retains ability to re-promote or promote arbitrary ci.yml runs.

## Files Modified

- `.github/workflows/promote-web.yml` (main implementation)
- `.github/workflows/build-web.yml` (reusable workflow extraction)

## Validation

- All syntax validated with actionlint
- Tested first production promotion (2026-06-08 15:42 UTC) with run_id empty
- Build + promote both succeeded, v1 deployed to web.edumips.org, HTTP 200 verified on /, /manifest.json, /v/1/, /prev/

## Next Step

Ready for routine use: `gh workflow run promote-web.yml --ref master` (empty run_id mode) or with `--field run_id=<external-ci-run>` for rollback/re-promote.
