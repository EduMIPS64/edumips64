# Coverage is reported directly in the GitHub Actions job summaries, so the
# Codecov integration must not post pull request comments or status checks.
codecov:
  require_ci_to_pass: false

comment: false

github_checks:
  annotations: false

coverage:
  status:
    project: false
    patch: false
